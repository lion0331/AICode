<UserControl x:Class="AICodeAssistant.ToolWindows.ChatToolWindowContent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="400">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="*"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		<ScrollViewer Grid.Row="0" Name="ChatScrollViewer" VerticalScrollBarVisibility="Auto">
			<StackPanel Name="ChatPanel" Margin="10" />
		</ScrollViewer>

		<DockPanel Grid.Row="1" Margin="10">
			<Button Name="SendButton" Content="发送" DockPanel.Dock="Right" Width="75" Margin="5,0,0,0" Click="SendButton_Click"/>
			<TextBox Name="InputTextBox" AcceptsReturn="True" Height="60" KeyDown="InputTextBox_KeyDown"/>
		</DockPanel>
	</Grid>
</UserControl>